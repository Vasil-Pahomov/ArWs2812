#ifndef anim_sparkr_h
#define anim_sparkr_h

#include "anim_glow.h"

/*
//probability of spark when in idle plase
#define SPARK_PROB 3

class AnimSparkr : public AnimGlow {
protected:
    //sequence of LEDS
    byte seq[LEDS];

    Color curColor = Color(0);
    Color prevColor = Color(0);
    int phase;
    
    void initSeq();
    
    void shuffleSeq();

    virtual void setUp();
    virtual void runImpl();

    const Color sparkleColor = Color(0xFFFFFF);
};
*/

#endif